<%
    Integer idUsuarioSesion = (Integer) session.getAttribute("idUsuario");
    String rolSesion = (String) session.getAttribute("rol");
    String nombreSesion = (String) session.getAttribute("nombre");
    String ctxSeguridad = request.getContextPath();
    if (idUsuarioSesion == null) {
        response.sendRedirect(ctxSeguridad + "/login.jsp?error=sesion");
        return;
    }
    boolean autorizado = false;
    for (String rolPermitido : rolesPermitidos) {
        if (rolPermitido.equals(rolSesion)) { autorizado = true; break; }
    }
    if (!autorizado) {
        response.sendRedirect(ctxSeguridad + "/acceso-denegado.jsp");
        return;
    }
%>